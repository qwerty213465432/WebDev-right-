const BIRTHDAY = '18.04.1982';  //We generally use upper case for constants that are “hard-coded”.

const AGE = someCode(BIRTHDAY);// Or, in other words, when the value is known prior to execution and directly written into the code.